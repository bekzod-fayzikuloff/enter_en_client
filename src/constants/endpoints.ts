import {config} from "../config";

export const loginUrl = `${config.apiUrl}/api/auth/login/`
export const registerUrl = `${config.apiUrl}/api/auth/register/`
export const refreshTokenUrl = `${config.apiUrl}/api/auth/refresh/`
export const studentsListUrl = `${config.apiUrl}/api/students/`
export const groupsListUrl = `${config.apiUrl}/api/groups/`